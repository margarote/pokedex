abstract class AppAssets {
  static const _imagesPath = 'assets/images';
  static const _iconsPath = 'assets/icons';
}

abstract class AppImages {
  static const ball = '${AppAssets._imagesPath}/ball.svg';
}

abstract class AppIcons {
  static const search = '${AppAssets._iconsPath}/search.svg';
  static const filter = '${AppAssets._iconsPath}/filter.svg';
}
